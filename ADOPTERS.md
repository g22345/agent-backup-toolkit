# Adopters

No external adopters are claimed yet.

After the project is public, genuine users may add a short entry by pull request:

```text
- Organization or GitHub handle — platform — destination type — public reference (optional)
```

Do not add customer names, private repositories, bucket names, backup IDs,
credentials, internal paths, or unverified endorsements.

